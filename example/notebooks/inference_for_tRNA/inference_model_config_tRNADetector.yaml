inference:
  batch_size: 16
  device: cpu
  max_length: 512
  num_workers: 4
  output_dir: ./results
  use_fp16: false
task:
  label_names:
  - Not tRNA
  - Partial tRNA
  - tRNA
  num_labels: 3
  task_type: multiclass
