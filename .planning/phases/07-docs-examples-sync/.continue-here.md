---
context: phase
phase: 07-docs-examples-sync
task: null
total_tasks: 15
status: paused
last_updated: 2026-05-15T09:03:50Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Mirror sync — `docs/example/` must remain byte-identical mirror of `example/` (except runtime artifacts). Any fix to `example/` files must be synced to `docs/example/`, and vice versa.
- [ ] CONSTRAINT: Import path no-op — `from dnallm import load_model_and_tokenizer` IS valid (exported in `__init__.py`). Do NOT "fix" valid imports.
- [ ] CONSTRAINT: CLI command partial no-op — `dnallm-train`, `dnallm-inference`, `dnallm-mcp-server` ARE valid standalone scripts (per `pyproject.toml`). Only `dnallm-benchmark` is non-existent.
- [ ] CONSTRAINT: MCP tool names — Server exposes `dna_sequence_predict` (NO underscore prefix). Internal Python methods use `_` prefix but exposed names do not.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Bulk regex replacement without per-file review | Documentation prose may legitimately reference strings being fixed (e.g., "binary classification" in explanatory text) | blocking | grep generates candidates → open each file → confirm context → modify |
| Fixing docs/example/ without fixing example/ | Breaks the mirror constraint; docs/example/ must stay byte-identical to example/ | blocking | Always include both paths in files_modified; use cp to sync after modification |
| Changing code to match docs | Backward compatibility constraint: fix docs to match code, not vice versa (except benchmark.py bug which is a clear code defect) | blocking | Verify source code is the defect before modifying dnallm/ files |
| Assuming all SPEC acceptance criteria are correct | SPEC.md REQ-10 and REQ-11 had factual errors corrected during planning | advisory | Cross-reference RESEARCH.md findings before implementing acceptance criteria literally |

## Current State

Phase 07 plan-phase is **COMPLETE**. 5 executable GSD-format plans have been created, verified (0 blockers), and are ready for execution.

- Phase directory: `.planning/phases/07-docs-examples-sync/`
- Plans: `07-01-PLAN.md`, `07-02-PLAN.md`, `07-03-PLAN.md`, `07-04a-PLAN.md`, `07-04b-PLAN.md`
- Research: `07-RESEARCH.md` (completed, open questions resolved)
- Validation: `07-VALIDATION.md` (created)
- SPEC: `07-SPEC.md` (updated — REQ-10/REQ-11 acceptance criteria corrected)

## Completed Work

- Task 1: Create 07-SPEC.md with 12 requirements — Done (ambiguity 0.16)
- Task 2: Complete spec-phase — Done
- Task 3: Complete discuss-phase — write 07-CONTEXT.md and 07-DISCUSSION-LOG.md — Done
- Task 4: Run plan-phase — Done (research + planning + verification)
  - Spawned gsd-phase-researcher → created 07-RESEARCH.md
  - Spawned gsd-planner → created 5 executable plans
  - Spawned gsd-plan-checker (2 rounds) → passed with 0 blockers
  - Applied revisions: split 07-04 into 07-04a/07-04b, updated SPEC.md, resolved RESEARCH.md questions

## Remaining Work

- Task 5: Execute Plan 07-01 — Fix benchmark.py bug and TaskConfig regex
- Task 6: Execute Plan 07-02 — Fix example YAML configs, notebooks, marimo, MCP
- Task 7: Execute Plan 07-03 — Fix API docs, imports, signatures, config keys, CLI, MCP docs
- Task 8: Execute Plan 07-04a — Create validation scripts (sync check, snippet validator)
- Task 9: Execute Plan 07-04b — Wire validation into CI + YAML load tests + final integration

## Decisions Made

- **D-01 Validation:** Local full notebook execution; CI import+initialization only (non-blocking)
- **D-02 Modification:** All fixes per-file reviewed, no script-based bulk replacements
- **D-03 TaskType:** Expand TaskConfig regex to accept both short and full names; keep enum values
- **D-04 Order:** Pattern-based traversal (source → YAML → imports → config → API signatures → CLI → task types → MCP → notebooks → API docs → tutorials → CI)
- **SPEC correction:** REQ-10 acceptance criteria narrowed to only `dnallm-benchmark`; REQ-11 corrected to confirm `dna_sequence_predict` WITHOUT underscore

## Blockers

No blockers.

## Required Reading (in order)

1. `.planning/phases/07-docs-examples-sync/07-SPEC.md` — locked requirements (note corrected REQ-10/REQ-11)
2. `.planning/phases/07-docs-examples-sync/07-CONTEXT.md` — implementation decisions
3. `.planning/phases/07-docs-examples-sync/07-RESEARCH.md` — verified findings and pitfalls
4. `.planning/phases/07-docs-examples-sync/07-01-PLAN.md` — first plan to execute

## Infrastructure State

- No background processes running
- Git branch: dev
- No uncommitted code changes in project files
- `.planning/` directory is gitignored (intentional — stays on dev branch)

## Next Action

Start with: Run `/gsd:execute-phase 07` to begin executing Plan 07-01 (fix benchmark.py class-vs-instance bug and expand TaskConfig regex).
